<p><label for="{{ name | to_id }}">{{ spec.label }} </label><span class="form--hint">{{ spec.hint }} </span></p>{% if spec.type == "string" and spec.template == true %}<div data-controller="liquid"><p><input data-action="input->liquid#render" data-target="liquid.input" id="{{ name | to_id }}" name="{{ name }}" placeholder="{{ spec.placeholder }}" type="text" value="{{ value | escape }}"></p><pre data-target="liquid.output"><code></code></pre><p></p></div>{% elsif spec.type == "text" and spec.template == true %}<div data-controller="liquid"><details><summary>see data sample</summary><p><textarea data-target="liquid.parameters" data-action="input->liquid#render input->auto-expand#expand" data-controller="auto-expand">{{ spec.template_parameters | escape }}</textarea></p></details><p><textarea data-action="input->liquid#render input->auto-expand#expand" data-controller="auto-expand" data-target="liquid.input" id="{{ name | to_id }}" name="{{ name }}" placeholder="{{ spec.placeholder }}">{{  value | escape }}</textarea></p><pre data-target="liquid.output"><code></code></pre><p></p></div>{% else %}<p>unsupported ({{ spec.type }})</p>{% endif %}